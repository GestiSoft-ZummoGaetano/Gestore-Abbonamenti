<UserControl x:Class="Gestore_Abbonamenti.View.UserControl.RightContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">

    <Grid Margin="5,5,5,0">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Anagrafica Genitore -->
        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10" Background="{Binding Genitore.Color, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Grid Margin="10" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NOME: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.NomeGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="COGNOME: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.CognomeGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NATO A: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.NascitaGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="DATA NASCITA: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.DataNascitaGenitore, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat=\{0:dd/MM/yyyy\}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="C.FISCALE: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.CodFiscaleGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="PAESE: " FontWeight="Bold" Margin=" 10,0,0,0"/>
                    <TextBlock Text="{Binding Genitore.PaeseGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center" Grid.ColumnSpan="2">
                    <TextBlock Text="INDIRIZZO: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.IndirizzoGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Width="auto" VerticalAlignment="Center" Margin="90,0,0,0">
                    <TextBlock Text="N°: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.NCivicoGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center" Grid.ColumnSpan="2">
                    <TextBlock Text="IBAN: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.IbanGenitore, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Dati Studente -->
        <Border Grid.Column="1" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Padding="10" Background="{Binding Genitore.Color, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="30"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NOME: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.NomeFiglio, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="COGNOME: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.CognomeFiglio, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="NATO A: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.NascitaFiglio, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="IMPORTO RIDOTTO DA LIQUIDARE: " FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Genitore.ImportoDaLiquidare, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat='€ {0:N2}'}" 
           Language="it-IT"  Margin="5,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ISTITUTO SCOLASTICO: " FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Genitore.Istituto, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               TextWrapping="Wrap" MaxWidth="220" Margin="-60,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" VerticalAlignment="Center" Grid.ColumnSpan="2">
                    <TextBlock Text="PERCENTUALE DI RIDUZIONE APPLICATA: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding Genitore.TipologiaPagamento, RelativeSource={RelativeSource AncestorType=UserControl}}" Margin="6,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>
