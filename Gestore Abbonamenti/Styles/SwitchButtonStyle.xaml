<ResourceDictionary xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors">

    <Style x:Key="CustomToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Width" Value="60"/>
        <Setter Property="Height" Value="25"/>
        <Setter Property="Background" Value="Red"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <!-- Sfondo dello switch -->
                        <Border x:Name="SwitchBackground"
                                Width="50" Height="25"
                                CornerRadius="15"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <!-- Icona ✔️ e ❌ -->
                                <TextBlock x:Name="CheckIcon" Text="✔" 
                                           Foreground="White" FontSize="15"
                                           HorizontalAlignment="Left" VerticalAlignment="Center"
                                           Margin="10,0,0,0" Visibility="Collapsed"/>
                                <TextBlock x:Name="CrossIcon" Text="✖"
                                           Foreground="White" FontSize="15"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"
                                           Margin="0,0,10,0"/>
                            </Grid>
                        </Border>

                        <!-- Pulsante Mobile -->
                        <Ellipse x:Name="Thumb" Width="20" Height="20"
                                 Fill="White" Stroke="Gray" StrokeThickness="1"
                                 HorizontalAlignment="Left"
                                 Margin="5,0,0,0">
                            <Ellipse.RenderTransform>
                                <TranslateTransform x:Name="ThumbTransform"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>
                    </Grid>

                    <!-- Stati Visivi -->
                    <ControlTemplate.Triggers>
                        <!-- Quando il ToggleButton è selezionato -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchBackground" Property="Background" Value="Green"/>
                            <Setter TargetName="Thumb" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform X="30"/>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CrossIcon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
